# Chat Oracle - Bug Tracker

## 已修复

### BUG-001: 聊天未带历史上下文
- 严重级别: 高
- 结果: 已修复，聊天会从数据库装配会话上下文再请求模型。

### BUG-002: Subscription 查询未复用请求级 session
- 严重级别: 中
- 结果: 已修复，统一走依赖注入 session。

### BUG-003: SSE 代理缓冲导致前端不实时
- 严重级别: 高
- 结果: 已修复，代理配置已支持流式刷新。

### BUG-004: `/chat` 页面和 API 结构不一致
- 严重级别: 高
- 结果: 已修复，聊天页已按真实接口重写。

### BUG-005: 本地模型不可达时系统返回 mock 结果
- 严重级别: 高
- 结果: 已修复，改为真实健康检查和远端回退。

### BUG-006: 免费订阅长期有效会覆盖付费订阅
- 严重级别: 高
- 结果: 已修复，当前订阅按 `start_at + end_at + priority_level` 判定，续费也支持未来生效。

### BUG-007: 没有支付订单台账，支付成功无法审计
- 严重级别: 高
- 结果: 已修复，新增 `payment_orders` 表和后台订单查询。

### BUG-008: 用户侧没有删除会话入口
- 严重级别: 中
- 结果: 已修复，聊天页会话列表已加入删除按钮。
