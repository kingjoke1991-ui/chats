# Project Progress

## 2026-04-18

当前系统已经从“后端 demo”推进到可运营的第一版商业骨架。

本轮完成：

- 接入 `mode.md` 本地模型和 `models.md` 远端模型，启动时做健康检查并自动回退
- `/chat` 重写为正式用户入口，支持登录、注册、真实 SSE、历史会话、删除会话
- `/admin` 重写为运营后台，支持用户、节点、套餐、订单、会话和失败消息查询
- 接入 `BEpusdt` 订阅支付真实流程
- 新增 `payment_orders` 台账表和回调激活订阅逻辑
- 后台支持套餐与价格管理
- 修复免费订阅长期覆盖付费订阅的问题，当前订阅按优先级和生效时间判定

## 当前状态

已实现：

- 认证
- 会话与消息持久化
- 聊天非流式与 SSE
- 用户前台
- 管理后台
- 模型回退
- 订阅支付创建订单
- 支付回调 / 主动查单同步订单并生效订阅

仍待继续建设：

- 用量账本与配额精算
- Redis 级限流与并发占位
- 审计日志
- 邮件验证 / 找回密码
- 监控与告警
